import {
  getCatastroDatosNoProtegidos,
  getReferenciasCatastrales,
  getMunicipios
} from '../catastro.api';

describe('Catastro Api', () => {
  test('Hello!', () => {
    expect(2 + 2).toBe(4);
  });
});
